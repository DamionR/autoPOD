import { render, screen } from "@testing-library/react";
import App from "./App";

test("renders App root without crashing", () => {
  render(<App />);
  // Add more specific tests as components are implemented
});
